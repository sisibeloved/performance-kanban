# 更新日志

本项目的所有重要更改均记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

---

## [0.3.8] - 2026-06-07

### 修复

- **JPG 导出未含几何平均行**：导出图片现在和界面表格一致，在最底部追加几何平均行(加粗、上方有分隔线、Speedup 列着色)；无 CJK 字体时标签回退为 "Geomean"
- **JPG 一级表头看不到分隔线**：一级(分组)表头蓝底原本组内外边框都被隐藏，现保留**分组之间**与外框的黑色分隔线(仅隐藏多列分组的组内竖线以呈现合并)，与二级表头/数据区的网格线一致

---

## [0.3.7] - 2026-06-05

### 变更

- **排序改用原生 Streamlit 按钮**：对比表上方一排按钮(用例名 + 各候选 Speedup)排序，激活列带 ▲/▼、点同列切换升降序。取代此前"点击表头 + 组件 iframe JS 桥"方案——后者在反代/CDN/CSP 部署环境下因懒加载 chunk 加载失败(大量 `X is not defined`)而失效。原生按钮不依赖 JS/iframe/懒加载 chunk，跨环境稳定；排序仍为服务端、表格/柱状图/导出共用，几何平均恒置底

---

## [0.3.6] - 2026-06-05

### 修复

- **弃用告警重现**：点击表头排序的 JS 桥误用了已弃用的 `st.components.v1.html`(将于 2026-06-01 后移除)，改用 `st.iframe` 承载桥接脚本(`height=1`，实际不可见)，消除告警；排序功能不变

---

## [0.3.5] - 2026-06-05

### 修复

- **表头排序在部署环境失效**(issue #4)：根因是 Speedup 柱状图把 MultiIndex 元组 `('', '用例名')` 当作索引名传给 `st.bar_chart`，Vega-Lite 将其解析为字段访问路径并抛 `Access path missing closing bracket`，该异常中断了整次重跑渲染，连带使点击表头排序看似无反应。改为用普通字符串索引名/列名构建图表数据(`build_speedup_chart_df`)

---

## [0.3.4] - 2026-06-05

### 新增

- **点击表头排序(平滑，不整页刷新)**：对比表二级表头(用例名 / 各候选 Speedup)可点击排序，箭头指示当前方向。机制为隐藏 `st.button` + `components.html` iframe 内事件委托桥接，触发常规 websocket 重跑。排序在服务端进行，表格 / 柱状图 / 导出共用同一结果；几何平均行始终置底、不参与排序
- **JPG 导出二级表头**：导出图片改用与界面表格一致的二级表头(一级分组为 baseline / 各候选名称跨列居中，二级为 值 / Speedup)，长名称自动撑宽分组、不溢出

### 修复

- **一级表头滚动不固定**：纵向滚动时一级(分组)表头与二级表头分层吸顶(level0 `top:0`、level1 `top:30px`)，两行都常驻
- **JPG 中文表头变方框**：修正 CJK 字体探测(原把纯拉丁字体 Droid Sans 误判为中文字体)，改为校验字体同时含中文与拉丁字形，无合格字体时回退 ASCII 表头

### 文档

- `AGENTS.md`：记录 `pkill -f streamlit` 自杀陷阱、跑起来验证的正确姿势、headless Chrome + CDP 交互验证法，以及点击表头排序需用事件委托(否则只有第一次点击生效)

---

## [0.3.3] - 2026-06-05

### 修复

- **对比表格占满屏幕**(issue #2):表格改为 `width:100%` + `<colgroup>` 控制列宽——窄于屏幕时拉伸填满,宽于屏幕时退化为最小宽度并横向滚动;固定列(用例名 + baseline)保持精确像素宽以保证 sticky 偏移正确,非固定列平分剩余空间。顺带修复 baseline 值列因 colspan 表头被挤压而截断的问题
- **JPG 导出文字溢出**(issue #3):导出图片按各列最长内容分配列宽并截断超长文本,表头与单元格内容不再戳出表格

---

## [0.3.2] - 2026-06-05

### 修复

- **对比表改用 HTML 表格渲染**:彻底解决 `st.dataframe`(canvas)的渲染层限制——固定的 baseline 列保留完整二级表头、一级表头真正居中、值/单位列宽合理;固定列(用例名 + baseline)用 CSS `position: sticky`,横向滚动时常驻
- **几何平均行回归**:重新在对比表最底部常驻几何平均行(加粗、受筛选影响、不受排序影响)
- **清理无效样式与死代码**:移除 `st.dataframe` 下不生效的表头居中 `set_table_styles` 与未使用的列宽计算

### 测试

- 用断言**最终 HTML 结构**(二级表头 `colspan`、居中、`sticky`、几何平均行)替换此前"只检查 Styler 对象是否含某段 CSS"的假信心测试

### 文档

- 新增 `AGENTS.md`:记录 Streamlit 渲染层限制、验证纪律、测试约定等协作约定

---

## [0.3.1] - 2026-06-04

### 修复

- **导出按钮兼容性**：移除不稳定的 JPG 剪贴板复制，导出区保留导出 Markdown、复制 Markdown 到剪贴板、导出 JPG 三个并排统一样式按钮，并改用 `st.iframe` 避开 `st.components.v1.html` 弃用提示
- **对比表头对齐**：一级列头调整为居中显示，保持二级表头与数据列结构不变

---

## [0.3.0] - 2026-06-04

### 新增

- **导出到剪贴板**：导出区新增复制 Markdown / JPG 到剪贴板按钮，保留原文件下载按钮

### 修复

- **对比表格显示行为**：保留 baseline 值 / 单位列并固定在左侧，同时避免拍平表头导致二级表头丢失
- **导出排序隔离**：导出排序仅作用于 Markdown / JPG 导出内容，不再影响对比表格数据或展示

---

## [0.2.1] - 2026-06-04

### 新增

- **导出专用排序参数**：Markdown / JPG 导出支持传入排序列与方向，仅影响导出内容，不改变表格展示顺序

---

## [0.2.0] - 2026-06-03

### 修复

- **适配真实 pyperf JSON 格式**：benchmark name 从 `benchmarks[].metadata.name` 读取，回退兼容旧格式（`runs[].metadata.name`）
- **标签冲突消重**：多个文件产生相同标签时自动追加文件名，避免 MultiIndex 列重复导致 Styler 崩溃（Closes #1）
- **模拟数据对齐真实格式**：`generate_sample_data.py` 使用多 run + warmups + calibration run 结构

### 新增

- **一键导出 Markdown**：对比表 / 趋势表导出为 Markdown 格式，值+单位合并，Speedup 显著值加粗，含 Geomean Speedup 摘要
- **一键导出 JPG**：matplotlib 渲染带颜色的表格图片，Speedup 列绿/红着色与 UI 一致
- **用例选择器共享**：对比表格与 Speedup 柱状图共享同一个用例选择器，选中即展示，取消即隐藏

---

## [0.1.0] - 2026-05-30

### 新增

- **Speedup 倍数对比表格**：以 `baseline / candidate` 作为核心指标，二级表头按 candidate 分组（值 / 单位 / Speedup）
- **多文件横向对比**：同时加载多个 pyperf JSON 文件，逐用例横向比较不同 Python 版本 / 实现的性能
- **颜色编码**：Speedup 列根据可配置的提升阈值 / 回归阈值自动着色（绿色 = 提升，红色 = 回归）
- **排序与筛选**：支持按 Speedup 升序 / 降序排序，支持按阈值范围筛选用例
- **自选用例柱状图**：底部柱状图支持用户手动选择用例，避免全量用例单位差异导致的图表失真
- **用例替换合成（Tab 2）**：从不同 JSON 中选取单个用例结果，合成为一组新数据，自动参与对比
- **固定 baseline 趋势观察（Tab 3）**：锁定 baseline，展示多个 candidate 的 Speedup 变化趋势，表尾标记方向箭头（↑ ↓ ↔）
- **趋势折线图**：支持选择单个用例绘制 Speedup 趋势折线
- **数据精度**：Speedup 保留 4 位小数，用例值保留 2 位小数，单位保持 pyperformance 原始输出（us / ms / s）
- **模拟数据生成器**：`generate_sample_data.py` 生成 4 个测试 JSON（CPython 3.12 / 3.13 / 3.14 + PyPy）
- **单元测试**：覆盖 JSON 解析、Speedup 计算、对比表构建、趋势表构建、用例替换合成、阈值筛选
- **WSL 启动脚本**：`start.sh` 一键启动（kill 旧进程 + 启动 Streamlit）
